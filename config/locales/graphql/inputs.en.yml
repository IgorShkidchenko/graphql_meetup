en:
  graphql:
    inputs:
      user:
        sign_up_input:
          desc: Sign up user via email
          args:
            first_name: User's first name
            last_name: User's last name
            email: User's account email
            password: User's account password
            password_confirmation: User's account password confirmation
        sign_in_input:
          desc: Sign in user via email
          args:
            email: User's account email
            password: User's account password
        movie_id_input:
          desc: Add or Remove movie from favorites or watchlist
          args:
            movie_id: Movie's id
        list_movie_input:
          desc: Add or Remove movie from list
          args:
            movie_id: Movie's id
            list_id: List's id
        delete_input:
          desc: Delete object
          args:
            id: Object's id
        create_list_input:
          desc: Create list
          args:
            name: List's name
